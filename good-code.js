// good-code.js: 符合规范的代码
function main() {
    // 使用 Logger 而不是 console.log (假设有 logger 模块)
    // 实际项目中这里应该是 logger.info(...)
    // 这里为了演示，我们至少保证它符合 ESLint 规则
    const message = "Production ready code";
    return message;
}

main();
